---
phase: 05-deployment-verification-and-user-setup
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false
requirements:
  - IAC-01
  - IAC-02
  - IAC-03
  - IAC-04
  - IAC-05
  - IAC-06
  - IAC-07
  - IAC-08
  - HARD-01
  - HARD-02
  - HARD-03
  - HARD-04
  - HARD-05
  - HARD-06
  - HARD-07
  - DOCK-01
  - DOCK-02
  - DOCK-03
  - DOCK-04
  - DOCK-05
  - DOCK-06
  - DOCK-07
  - ODOO-01
  - ODOO-02
  - ODOO-03
  - ODOO-04
  - ODOO-05
  - PG-01
  - PG-02
  - PG-03
  - PG-04
  - PROXY-01
  - PROXY-02
  - PROXY-03
  - PROXY-04
  - PROXY-05
  - BACK-01
  - BACK-02
  - BACK-03
  - BACK-04
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "Local dry run passes all tests against Docker Compose staging stack"
    - "Production verification completes with all smoke tests passing"
    - "Team member accounts are created and can log in to production Odoo"
    - "Infrastructure audit confirms host hardening is intact"
    - "Backup verification confirms backup files exist in local storage and DO Spaces"
  artifacts: []
  key_links:
    - from: "odookit/scripts/run-production.sh"
      to: "production droplet"
      via: "SSH tunnel + Playwright tests"
      pattern: "PROD_ODOO_URL"
---

<objective>
Execute two-stage production verification: first run the full OdooKit test suite against the local Docker Compose staging stack (dry run), then execute the production verification orchestration against the live droplet via SSH tunnel.

Purpose: Confirm that all Phases 1-4 deliver a working, hardened, backed-up Odoo system with real user accounts -- the final end-to-end validation before handoff.

Output: Verified production system with team member accounts created and all audits passed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deployment-verification-and-user-setup/05-CONTEXT.md
@.planning/phases/05-deployment-verification-and-user-setup/05-01-SUMMARY.md
@.planning/phases/05-deployment-verification-and-user-setup/05-02-SUMMARY.md
@odookit/scripts/run-production.sh
@odookit/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage 1 — Local dry run verification</name>
  <files></files>
  <action>
Run the full OdooKit test suite against the local Docker Compose staging stack to confirm everything works before touching production.

1. `cd odookit`
2. Ensure local stack is running: `docker compose up -d --wait`
3. Wait for Odoo health: use the wait-for-odoo helper or poll http://localhost:8069/web/health until ready
4. Run full local test suite: `npm run test:local`
5. Verify all tests pass (or correctly skip based on environment)

If local tests fail, fix any issues before proceeding to production. This catches config mistakes before they hit the live system.
  </action>
  <verify>npm run test:local exits 0 with all tests passing or correctly skipping</verify>
  <done>Local dry run passes — all smoke, setup, audit, and workflow tests work against the staging stack.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Stage 2 — Production verification execution</name>
  <files></files>
  <action>
Human checkpoint: Execute production verification against the live droplet.

**What was built:** Complete production verification tooling — SSH tunnel scripts, backup verification, team member config-driven user creation, and orchestration script with fail-fast safety gates. Local dry run has passed.

**Pre-requisites (must be done before running):**
1. Ensure droplet is provisioned and accessible via SSH on port 9292 as `deploy` user
2. Copy `odookit/team-members.json.example` to `odookit/team-members.json` and fill in real user details and passwords
3. Ensure `odookit/.env` has correct values for INFRA_SSH_HOST, ADMIN_LOGIN, ADMIN_PASSWORD

**Execute production verification:**
```bash
cd odookit
npm run verify:prod
```

**Expected behavior:**
- SSH tunnel opens to droplet port 443
- Stage 1/5 (Smoke): Odoo health, login, and module checks pass through tunnel
- Stage 2/5 (Infra Audit): SSH hardening, UFW, fail2ban, Docker, auditd checks pass
- Stage 3/5 (Odoo Audit): Database manager disabled, modules installed, HTTP headers present
- Stage 4/5 (User Creation): Team members created from team-members.json, each verified with login
- Stage 5/5 (Backup): Backup triggered, status files show OK, sync to Spaces confirmed
- SSH tunnel closes automatically on exit
- HTML report generated in odookit/reports/

**Verify results:**
1. Check console output for PASS/FAIL per stage
2. Open `odookit/reports/index.html` for detailed Playwright report
3. Log in to Odoo as each team member to confirm access works
4. Verify team members can access CRM and Project modules but NOT Settings
  </action>
  <verify>All 5 stages complete. Smoke tests pass. Team members created. HTML report generated in odookit/reports/.</verify>
  <done>Production system verified end-to-end with real user accounts. Type "approved" with results, or describe any issues encountered.</done>
</task>

</tasks>

<verification>
1. Local dry run passes all tests
2. Production smoke tests pass through SSH tunnel
3. Infrastructure audit confirms hardening
4. Odoo audit confirms configuration
5. Team member accounts created and verified
6. Backup files confirmed in local storage and DO Spaces
7. HTML report generated
</verification>

<success_criteria>
- Two-stage verification complete: local dry run passed, production verification passed
- All team member accounts created and login-verified
- All 5 production verification stages complete (smoke blocking, others non-blocking)
- HTML report available in odookit/reports/
- System confirmed production-ready with real user accounts
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment-verification-and-user-setup/05-03-SUMMARY.md`
</output>
